
<nav class="navbar navbar-expand-lg navbar-light bg-light">
    <a class="navbar-brand" href="{{ path('home') }}">PARIS Events</a>
    <button class="navbar-toggler" type="button" data-toggle="collapse" data-target="#navbarColor03" aria-controls="navbarColor03" aria-expanded="false" aria-label="Toggle navigation">
        <span class="navbar-toggler-icon"></span>
    </button>

    <div class="collapse navbar-collapse" id="navbarColor03">
        <ul class="navbar-nav mr-auto">
            <li class="nav-item">
                <a class="nav-link" href="{{ path('event_index') }}">Événements</a>
            </li>
        </ul>

        <ul class="navbar-nav ml-auto">
            {%  if app.user %}
                <li class="nav-item dropdown">
                    <a href="#" class="nav-link dropdown-toggle" data-toggle="dropdown" id="accountDropdownLink">

                        {% if app.user.avatar %}
                            <img src="{{ app.user.avatar }}" class="avatar avatar-mini" alt="Avatar de {{ app.user.fullName }}">
                        {% else %}
                            <img src="{{ asset('img/avatar.jpg') }}" class="avatar avatar-mini" alt="Avatar de {{ app.user.fullName }}">
                        {% endif %}

                        {{ app.user.fullName }}
                    </a>

                    <div class="dropdown-menu dropdown-menu-right" aria-labelledby="accountDropdownLink">
                        <a href="{{ path('event_create') }}" class="dropdown-item">Ajouter un événement</a>
                        <a href="{{ path('user_favorite') }}" class="dropdown-item">Mes publications</a>
                        <a href="{{ path('user_favorite') }}" class="dropdown-item">Mes favoris</a>
                        <div class="dropdown-divider"></div>
                        <a href="{{ path('user_edit') }}" class="dropdown-item">Mon profile</a>
                        <a href="{{ path('user_edit') }}" class="dropdown-item">Editer mes infos</a>
                        <div class="dropdown-divider"></div>
                        <a href="{{ path('user_logout') }}" class="dropdown-item">Déconnexion</a>
                    </div>
                </li>
            {% else %}
                <li class="nav item">
                    <a href="{{ path('user_register') }}" class="nav-link">Inscription</a>
                </li>
                <li class="nav item">
                    <a href="{{ path('user_login') }}" class="nav-link">Connexion</a>
                </li>
            {% endif %}
        </ul>

    </div>
</nav>