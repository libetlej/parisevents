{% extends 'base.html.twig' %}

{% block title %}Editer événement{% endblock %}

{% block body %}
    <div class="container">

        <h2 class="my-5 text-center">Editer {{ event.title }}</h2>

        <div class="row">
            <div class="col-md-3"></div>
            <div class="col-md-6 col-sm-12">
                {{ form_start(form) }}
                {{ form_widget(form) }}
                <input type="submit" value="modifier" class="btn btn-success">
                {{ form_end(form) }}
            </div>
            <div class="col-md-3"></div>
        </div>

    </div>

{% endblock %}